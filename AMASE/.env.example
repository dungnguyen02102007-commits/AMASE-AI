# Anthropic API key — required for /api/analyze
ANTHROPIC_API_KEY=

# Supabase — Project Settings → API
# These are public-safe (browser-exposed), do NOT add the service_role key here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
